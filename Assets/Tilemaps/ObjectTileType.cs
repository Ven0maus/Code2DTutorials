namespace Assets.Tilemaps
{
    public enum ObjectTileType
    {
        /* 2001 - 3000 */
        Empty = 0, // We need an empty, because we don't want objects everywhere
        OakTree = 2001
    }
}
